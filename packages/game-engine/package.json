{
  "name": "@wordle-royale/game-engine",
  "version": "0.0.0",
  "private": true,
  "description": "Pure deterministic gameplay, scoring, standings, and rating logic.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "node --check src/index.ts && node --check tests/index.test.ts",
    "test": "node --test tests/*.test.ts",
    "build": "pnpm run typecheck"
  }
}
