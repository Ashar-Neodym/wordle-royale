{
  "name": "@wordle-royale/rating-tools",
  "version": "0.0.0",
  "private": true,
  "description": "MMR scenario and simulation tooling.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --test src/**/*.test.ts",
    "build": "pnpm typecheck",
    "rating:simulate": "node src/cli.ts simulate"
  },
  "devDependencies": {
    "@types/node": "^24.0.7",
    "typescript": "^5.8.3"
  }
}
