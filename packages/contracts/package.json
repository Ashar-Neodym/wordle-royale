{
  "name": "@wordle-royale/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript/Zod contracts, enums, API envelopes, and event names.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "node --test src/**/*.test.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "@types/node": "^24.10.0"
  }
}
